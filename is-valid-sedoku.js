/**
 * @param {character[][]} board
 * @return {boolean}
 */
const isValidSudoku = (board) => {

};

/**
 * provided the x, y, this will return an int indicating the index of the sub box that
 * the coordinate exist at.
 * 
 * top left -> 0
 * top right -> 2
 * ...
 * center -> 4
 * ...
 * bottom right -> 8
 * @param {int} x 
 * @param {int} y 
 */
const getGridSubBox = (x, y) => {
  // [
  //   [0, 0], [1, 0], [2, 0],
  //   [0, 1], [1, 1], [2, 1],
  //   [0, 2], [1, 2], [2, 2]
  // ]
}
